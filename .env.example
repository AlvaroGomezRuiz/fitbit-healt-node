# --- Scripts Python (Drive / OAuth) ---
# ID de la carpeta raíz "SALUD" en Google Drive (de la URL al abrirla).
GOOGLE_DRIVE_SALUD_ROOT_FOLDER_ID=

# Opcional: JSON completo del token OAuth (alternativa a token.json en la raíz).
# GOOGLE_OAUTH_TOKEN_JSON=

# --- Next.js (frontend/): copia variables a frontend/.env.local según documentación del front ---
